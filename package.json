{
  "name": "stream-cam",
  "module": "server.ts",
  "type": "module",
  "scripts": {
    "start": "echo 'PRODUCTION | Starting stream-cam servers...' && ENVIRONMENT=PRODUCTION bun server.ts & ENVIRONMENT=PRODUCTION bun uiServer.ts &",
    "dev": "echo 'DEVELOPMENT | Starting stream-cam servers...' && ENVIRONMENT=DEVELOPMENT bun server.ts & ENVIRONMENT=DEVELOPMENT bun uiServer.ts &",
    "stop": "echo 'Stopping stream-cam servers...' && sudo fuser -k 20240/tcp & sudo fuser -k 80/tcp",
    "prep": "echo 'Preparing stream-cam for push...' && echo '[]' > ./mediamtx/config/settings.json && echo '' > ./mediamtx/config/current.txt"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "uuid": "^11.0.5",
    "yaml": "^2.7.0"
  }
}